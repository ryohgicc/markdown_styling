# Markdown Poster Studio

A lightweight web app that renders Markdown into shareable poster styles and exports full-length PNG images.

## Features

- Live Markdown preview
- Multiple visual poster themes
- Full-length poster export as PNG
- Responsive layout for desktop and mobile

## Files

- `index.html`: app structure
- `style.css`: themes and layout
- `main.js`: Markdown rendering and export logic
